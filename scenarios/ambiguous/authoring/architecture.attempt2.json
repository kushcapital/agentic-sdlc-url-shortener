{
  "summary": "Two independent hardenings. (1) Generated code length: shortener.code-length 7 -> 8. (2) Custom alias minimum length 4 -> 6 at creation. The trap in (2): LinkRules.CUSTOM_ALIAS_REGEX validates aliases at creation, and the same 4-32 regex is hard-coded in the controllers' @GetMapping path templates; raising the creation minimum is fine, but tightening the path templates would make legacy 4/5-char aliases 404. Split: CUSTOM_ALIAS_REGEX (creation, 6-32) and a new LinkRules.CODE_PATH constant (resolution, 4-32) used by the controllers.",
  "components": [
    {
      "name": "ShortenerProperties / application.yml",
      "responsibility": "codeLength 7 -> 8",
      "path": "src/main/java/dev/rajeev/shortener/config/ShortenerProperties.java"
    },
    {
      "name": "RandomCodeGenerator",
      "responsibility": "Unchanged; length is injected",
      "path": "src/main/java/dev/rajeev/shortener/domain/RandomCodeGenerator.java"
    },
    {
      "name": "LinkRules",
      "responsibility": "CUSTOM_ALIAS_REGEX (creation) and CODE_PATH (path templates)",
      "path": "src/main/java/dev/rajeev/shortener/domain/LinkRules.java"
    },
    {
      "name": "Controllers",
      "responsibility": "Use LinkRules.CODE_PATH in path templates; the request record keeps @Pattern(CUSTOM_ALIAS_REGEX)",
      "path": "src/main/java/dev/rajeev/shortener/web/LinkController.java, src/main/java/dev/rajeev/shortener/web/RedirectController.java"
    }
  ],
  "impactedModules": [
    {
      "path": "src/main/java/dev/rajeev/shortener/config/ShortenerProperties.java",
      "changeType": "modify",
      "reason": "@DefaultValue 8."
    },
    {
      "path": "src/main/resources/application.yml",
      "changeType": "modify",
      "reason": "shortener.code-length: 8 (explicit value overrides the record default)."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/domain/RandomCodeGenerator.java",
      "changeType": "none",
      "reason": "Parameterised by length."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/web/LinkController.java",
      "changeType": "none",
      "reason": "Path template accepts 4-32; 8 fits."
    },
    {
      "path": "src/test/java/dev/rajeev/shortener/web/ApiIntegrationTest.java",
      "changeType": "modify",
      "reason": "Assertion on code length."
    },
    {
      "path": "src/test/java/dev/rajeev/shortener/config/ShortenerPropertiesTest.java",
      "changeType": "create",
      "reason": "Pin the bound default."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/domain/LinkRules.java",
      "changeType": "modify",
      "reason": "Raise alias minimum to 6; add CODE_PATH for resolution."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/domain/CreateLinkRequest.java",
      "changeType": "modify",
      "reason": "Validation message."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/domain/LinkService.java",
      "changeType": "modify",
      "reason": "Validation message."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/web/LinkController.java",
      "changeType": "modify",
      "reason": "Path templates use LinkRules.CODE_PATH."
    },
    {
      "path": "src/main/java/dev/rajeev/shortener/web/RedirectController.java",
      "changeType": "modify",
      "reason": "Path template uses LinkRules.CODE_PATH."
    },
    {
      "path": "src/test/java/dev/rajeev/shortener/web/GuessabilityIntegrationTest.java",
      "changeType": "create",
      "reason": "AC-1, AC-2, AC-3 acceptance tests."
    },
    {
      "path": "src/test/java/dev/rajeev/shortener/domain/LinkServiceTest.java",
      "changeType": "modify",
      "reason": "Rule split unit test."
    }
  ],
  "dataFlow": [
    "POST /api/links -> LinkService.create -> RandomCodeGenerator(props.codeLength()=8) -> 8-char code",
    "POST /api/links with customAlias -> LinkService validation (CUSTOM_ALIAS_PATTERN 6-32) -> 400 VALIDATION if too short",
    "GET /{code} -> path template {code:CODE_PATH} (4-32) -> LinkService.resolve -> legacy aliases still found"
  ],
  "apiChanges": [
    {
      "method": "POST",
      "path": "/api/links",
      "change": "Generated codes are 8 chars (was 7). Not a contract change; clients must not assume a length."
    },
    {
      "method": "POST",
      "path": "/api/links",
      "change": "customAlias now requires 6-32 characters (was 4-32). Breaking for clients creating short aliases; documented."
    }
  ],
  "schemaChanges": [],
  "decisions": [
    {
      "id": "D-1",
      "decision": "Raise entropy by length (8 chars) rather than by alphabet.",
      "rationale": "Base62 is already the largest URL-safe unambiguous alphabet; one more character multiplies the keyspace by 62.",
      "alternatives": [
        "Base64url (adds - and _; marginal gain)",
        "Longer codes (10+)"
      ],
      "tradeoffs": "Slightly longer URLs."
    },
    {
      "id": "D-2",
      "decision": "Separate the creation-time alias rule from the path-resolution rule (LinkRules.CUSTOM_ALIAS_REGEX vs LinkRules.CODE_PATH).",
      "rationale": "Issued codes are a contract; the resolution pattern must stay as wide as anything ever issued. Only creation gets stricter.",
      "alternatives": [
        "Tighten one shared pattern (breaks legacy aliases)",
        "Migrate legacy aliases (rewrites shared URLs)"
      ],
      "tradeoffs": "Two patterns to keep in mind; documented in code comments and ADR."
    }
  ],
  "securityConsiderations": [
    "Random codes use crypto.randomBytes with rejection sampling; unchanged.",
    "Alias minimum length is a validation rule, not a security boundary; a denylist for common words is a possible follow-up."
  ],
  "rollbackStrategy": "Revert both; nothing issued under the new rules becomes unresolvable because the path templates are unchanged (4-32)."
}