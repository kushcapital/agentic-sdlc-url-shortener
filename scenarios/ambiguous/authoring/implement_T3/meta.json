{ "taskId": "T3", "summary": "Custom aliases must be at least 6 characters at creation, while URL-path resolution keeps accepting 4-32 so every code ever issued still resolves: CUSTOM_ALIAS_REGEX (creation) is split from a new CODE_PATH constant used by the controllers' path templates. Unit test for both rules.", "notes": ["Splitting the two rules is what prevents this hardening from breaking legacy 4/5-char aliases."] }
