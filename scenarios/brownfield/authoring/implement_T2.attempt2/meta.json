{ "taskId": "T2", "summary": "Domain + persistence for expiry: Link.expiresAt with expiredAt(now), CreateLinkRequest.expiresAt, LinkResponse.expiresAt, SchemaMigrator v2 (additive nullable column, idempotent, forward-compatible), JdbcLinkRepository column mapping, contract/migrator/service tests updated. No files outside src/.", "notes": ["Removed the .env.local from the previous attempt per policy feedback (path-allowlist)."] }
