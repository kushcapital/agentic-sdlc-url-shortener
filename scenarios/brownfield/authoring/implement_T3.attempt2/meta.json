{ "taskId": "T3", "summary": "Enforce and expose expiry: ErrorCode gains EXPIRED (mapped to 410 in ApiExceptionHandler.statusFor); LinkService rejects a past expiresAt (VALIDATION) and resolve() throws EXPIRED against the injected clock; service unit tests with the fake clock.", "notes": ["Fixes the compile error from attempt 1 by adding EXPIRED to the exhaustive status switch in ApiExceptionHandler (protected file — needs reviewer approval)."] }
